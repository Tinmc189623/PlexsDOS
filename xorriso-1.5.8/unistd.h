/* ==========================================================================
 * unistd.h — Windows MSVC stub
 * 项目：xorriso 1.5.8 Windows 移植
 * 作者：Tinmc189623 | 团队：Nexlyh | 品牌：Nexsteaduser
 *
 * MSVC 不提供 <unistd.h>，此桩文件将 POSIX 声明重定向到
 * win-compat.h（通过 ForcedIncludeFiles 全局注入）。
 * 仅用于满足 #include <unistd.h> 的编译需求。
 * ========================================================================== */

#ifndef _UNISTD_H_
#define _UNISTD_H_

/* win-compat.h 已通过 ForcedIncludeFiles 全局注入，此处仅提供占位 */

#endif /* _UNISTD_H_ */
