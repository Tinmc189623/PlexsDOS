# Changelog

本文件记录 Nexsteaduser PlexsDOS 的所有重要变更。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 规范。

---

## [0.1.0] - 2025-07-25

### 架构定位修正（重要）

- **🔧 架构类型澄清**: 正式将 Plexs 内核定位为**标准宏内核（Monolithic Kernel）**。
  - 此前文档和官网中错误使用了"微内核"表述，现经代码审计确认：所有子系统（驱动、文件系统、Shell、GUI、编辑器、用户系统、调度器）均直接链接进内核二进制，在 Ring 0 特权级运行。
  - 子系统之间通过直接 C/C++ 函数调用交互，无 IPC 消息传递机制、无用户态服务器进程、无用户态/内核态地址空间分离。
  - 系统调用通过软中断 `INT 0x22` 实现，入口即内核函数，零上下文切换开销。
  - 所有文档（README、wiki、官网）已同步修正为"宏内核"描述。

### 内核代码优化（Plexs 宏内核）

- **✨ 新增启动状态辅助函数** (`kernel/kernel_main.c`):
  - 新增 `boot_ok(msg)` / `boot_skip(msg)` / `boot_fail(msg)` / `boot_info(msg)` 四个静态内联辅助函数。
  - 消除初始化序列中 **20+ 处** 重复的"设置颜色→打印标记→恢复颜色→打印消息→换行"四行模式。
  - 代码从 ~500 行精简至 ~470 行，可读性和可维护性大幅提升。
  - 统一颜色方案：`[OK]` 绿色(0x0A)、`[--]` 黄色(0x0E)、`[!!]` 红色(0x0C)、`[II]` 黄色(0x0E)。

- **✨ HAL 块设备注册重构**:
  - 将原来分散在三个独立 `{}` 块中的 FDC0/FDC1/ATA 操作结构体注册，合并为单一初始化块。
  - 使用 C99 指定初始化器（designated initializer）一次性初始化 ops 结构体，消除冗余赋值代码。
  - 减少 3 个单独的静态代码块。

- **🐛 ISA 枚举跳过场景补全提示**:
  - 从可移动介质（软盘/CD）启动时，跳过 ISA 枚举的分支原来静默无输出，现补充 `[II] ISA enumeration skipped (removable boot)` 提示信息，启动日志更完整。

- **🐛 CPU 厂商字符串输出格式修正**:
  - 修复 CPU 初始化行中 `CPU initialized ( GenuineIntel )` 多余空格问题，现输出为 `CPU initialized (GenuineIntel)` 格式。

### 文档更新

- **📝 README.md 全面重写**:
  - 明确标注"宏内核（Monolithic Kernel）"架构，补充宏内核特征说明（单地址空间、直接函数调用、静态链接、高性能、紧凑内核）。
  - 新增 ASCII 架构图，展示内核五大模块（引导加载/内核核心/驱动框架/文件系统/用户接口）。
  - 补充完整的功能特性表格（24 个子系统状态）、内置命令清单（28 条命令）。
  - 补充详细的技术栈表格（语言/工具链/编译器选项）。
  - 补充跨平台构建步骤（Linux / Windows MSYS2 / macOS）、QEMU 运行命令、GDB 调试方法。
  - 补充 CMake 构建选项说明表。
  - 补充完整的目录结构树、启动流程时序说明。
  - 补充 GPL v2 许可证声明、作者/团队/品牌信息（Tinmc189623 / Nexlyh / Nexsteaduser）。
  - 补充真实的 GitCode 和 Gitee 仓库地址。

- **📝 docs/wiki/Kernel-Architecture.md 架构文档修正**:
  - 更新架构图为宏内核架构，移除微内核相关的 IPC/用户态服务器等错误描述。
  - 准确描述所有子系统在内核态（Ring 0）运行的事实。
  - 说明系统调用机制、内存模型、驱动架构等宏内核特征。

- **🗑️ 删除占位文件**:
  - 删除 `README.en.md`（原内容为"中文 README 为主"的空壳占位文件，避免误导）。

### 官网修正

- **🎨 官网终端输出与真实内核同步**:
  - 官网 `BootTerminal.vue` 组件的启动日志从 `kernel_main.c` 实际代码提取，确保与真实启动输出一致。
  - 修正所有"微内核"描述为"宏内核"，包括 Hero 区域、特性卡片、架构图。
  - 修正驱动语言描述（驱动使用 C++ 而非纯 C）。
  - 修正分页内存描述（4KB 页面而非"虚拟内存"模糊表述）。

### 构建系统

- **🔧 无新增构建依赖**:
  - 所有代码改动保持零新增依赖，纯 C/C++/NASM 现有工具链即可构建。
  - C99 指定初始化器在 `-std=gnu99` 下完全兼容。

---

## [0.0.1] - 2025-07（历史版本）

### 初始版本特性

- ✅ 32 位 x86 保护模式引导（三阶段引导加载器）
- ✅ GDT/IDT/分页/异常处理基础设施
- ✅ PS/2 键盘鼠标驱动
- ✅ PCI / ATA / AHCI / FDC / CD-ROM 驱动
- ✅ FAT32 / ISO 9660 文件系统
- ✅ MBR 分区扫描、驱动器号管理（A:-Z:）
- ✅ 命令行 Shell（28+ 内置命令）
- ✅ EDIT.COM 全屏文本编辑器
- ✅ VBE 图形子系统框架
- ✅ 进程调度器框架
- ✅ 用户账户系统
- ✅ 系统安装程序
- ✅ HAL 硬件抽象层
- ✅ 串口调试输出（COM1 115200）
- ✅ CONFIG.SYS 配置解析
- ✅ 红屏死机（Kernel Panic）处理
- ✅ CMake + NASM + GCC 交叉编译构建系统
- ✅ QEMU 模拟器支持

---

## 版本说明

- **版本号格式**: `主版本.次版本.修订号`
- **主版本 0**: 早期开发阶段，API 和内核 ABI 不保证稳定
- **次版本 1**: 首个可用版本，具备基础交互能力和文件系统
- **修订号**: 修复和小改进

## 提交规范

提交信息遵循 Conventional Commits 规范：
- `feat:` 新功能
- `fix:` 修复
- `docs:` 文档更新
- `refactor:` 重构（不改变行为）
- `perf:` 性能优化
- `style:` 代码风格
- `test:` 测试相关
- `chore:` 构建/工具链相关

---

**Nexsteaduser PlexsDOS** · **Nexlyh Team** · **Tinmc189623**
*admin@nexsteaduser.com*
